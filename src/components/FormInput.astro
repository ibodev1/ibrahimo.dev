---
const { name, type = 'text', placeholder } = Astro.props;
---

<div>
  <label for={name} class="block text-sm font-medium text-light-400">
    {placeholder}
  </label>
  <input
    type={type}
    name={name}
    id={name}
    placeholder={placeholder}
    required
    class="mt-1 block w-full border border-primary bg-dark-500 text-light rounded-md py-2 px-3 shadow-sm focus:ring-primary focus:border-primary sm:text-sm"
  />
</div>
